import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: { main: '#ea580c' }, // Laranja Mercafácil
    secondary: { main: '#388e3c' },
    background: { default: '#f3f4f6' }
  }
});

export default theme;